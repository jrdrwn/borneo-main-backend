[project]
name = "vercel-fastapi-starter"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
  "fastapi>=0.121.0",
  "neo4j>=5.27.0",
  "pydantic>=2.10.4",
  "python-dotenv>=1.0.1",
  "uvicorn>=0.34.0",
]

[tool.vercel]
entrypoint = "main:app"